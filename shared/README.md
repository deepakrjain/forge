# Forge Shared Module

Shared Pydantic models, schemas, enums, and constants used across both `/api` and `/worker` packages.

## Installation for Local Development

To install in editable mode in python virtual environments:

```bash
pip install -e .
```
